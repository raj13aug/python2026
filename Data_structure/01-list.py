# * use to mutiple the index of list
# + use to combine both list() I mean string + boolen

list() # empty string
number = list(range(20))
charts = list("hello")

combined = number + charts
print(combined)

