# all function return the none value by default
# function works two ways --> carry out the task
#                         --> Return the task

def greet(name):
    return f"{name}"

message = greet("mano")
print(message)